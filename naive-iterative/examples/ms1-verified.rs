use vstd::prelude::*;
fn main() {}
verus!{

pub fn myfun(a: &mut Vec<usize>, sum: &mut Vec<usize>, N: usize) 
    requires 
    ensures
{
    let mut i: usize = 0;
    {
    }

    
    {
        if (i == 0) {
        } else {
            let temp = sum[0];
        }
    }
}
} // verus!